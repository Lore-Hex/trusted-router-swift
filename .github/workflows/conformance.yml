name: SDK conformance

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  conformance:
    uses: Lore-Hex/trusted-router-sdk-conformance/.github/workflows/sdk-pr.yml@main
    with:
      sdk: swift
      allowed_skips: none
