# GitGuardian configuration.
#
# Tests/TrustedRouterTests/Fixtures/receipts/ holds FROZEN PUBLIC TEST
# VECTORS for signed inference receipts: JWS tokens signed by a fixed,
# documented fixture key (generated by quill-cloud-proxy
# enclave-go/cmd/receipt-fixtures). They contain no credentials — the
# scanner flags the JWT-shaped strings.
version: 2
secret:
  ignored-paths:
    - "Tests/TrustedRouterTests/Fixtures/receipts/**"
